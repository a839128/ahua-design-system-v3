---
description: A token object to be shared across React and React Native
labels: ['tokens', 'design tokens']
---

import { ObjectFormatter } from '@teambit/code.ui.object-formatter';
import { baseTokens } from './base-tokens';

A component that seals a contract between designers and developers.

```js live
<ObjectFormatter style={{ marginBottom: 16 }} data={baseTokens} />
```
