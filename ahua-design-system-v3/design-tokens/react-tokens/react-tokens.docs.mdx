---
description: A token object to be shared across React Applications, based on Base Tokens.
labels: ['tokens', 'design tokens']
---

import { ObjectFormatter } from '@teambit/code.ui.object-formatter';
import { reactTokens } from './react-tokens';

A component that seals a contract between designers and developers.

```js live
<ObjectFormatter style={{ marginBottom: 16 }} data={reactTokens} />
```
